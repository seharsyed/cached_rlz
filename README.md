# Parser

Parser contains implementation of LZ parser and random-access RLZ
index.

# Refgen

Refgen contains a program that generates reference string of arbitrary
integer type for an input of that type.

# SA

SA contains a simple implementation of prefix doubling algorithm to
compute suffix arrays of arbitrary integer type for inputs of
arbitrary integer types.
